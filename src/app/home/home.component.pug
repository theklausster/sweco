doctype html
.home-wrapper
    .search-wrapper
        input(#lol, (keyup)="onSearch(lol.value)", placeholder="Søg i kontakter...")
    .list-wrapper
        .list-item(*ngFor="let item of contacts", (click)="itemClicked(item)", [class.active]="activeItem === item", [class.notActive]="activeItem !== item")
            //- her burde man lave en ny component og sende et object med som input
            .id {{item.id}}
            .name {{item.name}}
            i.fa.fa-info-circle